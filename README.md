### Perintah Termux :
    $ apt update -y && apt upgrade -y
    $ pkg install git
    $ pkg install python
    $ git clone https://github.com/RONI-404/OLD
    $ cd OLD
    $ python Old.py
### Free Not Prem
